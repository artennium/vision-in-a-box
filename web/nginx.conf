server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /api/ {
        # 'yolo-service' คือชื่อที่เราจะตั้งใน docker-compose
        proxy_pass http://yolo-service:8000/;
        proxy_set_header Host $host;
    }
}
